<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.ScheduleMapper">

    <select id="getSchedules" parameterType="int" resultType="com.example.demo.DTO.ScheduleDTO">

        SELECT * FROM schedule WHERE cinemaId = #{cinemaId}
        
    </select>

    <select id="getMovie" parameterType="int" resultType="com.example.demo.DTO.MovieDTO">
        select * FROM movie WHERE movieId = #{movieId}
    </select>

</mapper>